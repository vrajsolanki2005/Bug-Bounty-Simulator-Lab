APPENDIX - BUG BOUNTY SIMULATOR LAB
=====================================

A. VULNERABILITY REFERENCE
--------------------------
1. Stored XSS - FLAG{STORED_XSS_MASTER_2024}
2. Reflected XSS - FLAG{REFLECTED_XSS_FOUND}
3. SQL Injection - FLAG{SQL_INJECTION_BYPASSED}
4. CSRF Attack - FLAG{CSRF_TOKEN_MISSING}
5. XXE Injection - FLAG{XXE_PAYLOAD_SUCCESS}
6. LFI Attack - FLAG{LOCAL_FILE_INCLUSION}
7. RCE Exploit - FLAG{REMOTE_CODE_EXECUTION}
8. IDOR Bypass - FLAG{INSECURE_DIRECT_OBJECT_REF}
9. Auth Bypass - FLAG{BROKEN_AUTHENTICATION}
10. Session Hijack - FLAG{SESSION_HIJACKED}
11. File Upload - FLAG{FILE_UPLOAD_BYPASS}
12. Open Redirect - FLAG{OPEN_REDIRECT_FOUND}

B. COMMON PAYLOADS
------------------
XSS:
- <script>alert(1)</script>
- <img src=x onerror=alert(1)>
- <svg onload=alert(1)>

SQL Injection:
- ' OR '1'='1' --
- admin'--
- ' UNION SELECT 1,2,3--

CSRF:
- Remove CSRF tokens from forms
- Cross-origin form submissions

XXE:
- <!DOCTYPE foo [<!ENTITY xxe SYSTEM "file:///etc/passwd">]>

C. SCORING SYSTEM
-----------------
- Beginner Labs: 100 points
- Intermediate Labs: 120-160 points
- Advanced Labs: 170-250 points

D. API ENDPOINTS
----------------
Authentication:
- POST /register
- POST /login
- GET /api/user-profile

Labs:
- GET /lab/<id>
- POST /submit_flag
- POST /check_vulnerability

AI Assistant:
- POST /api/ai-chat
- GET /api/ai-hint/<lab_id>

E. ENVIRONMENT VARIABLES
------------------------
OPENAI_API_KEY=your_openai_key
GEMINI_API_KEY=your_gemini_key
SMTP_EMAIL=your_email
SMTP_PASSWORD=your_password

F. DATABASE SCHEMA
------------------
Users: id, username, name, email, password_hash, score, labs_completed
Vulnerabilities: id, title, description, difficulty, flag
UserFlags: user_id, vulnerability_id, flag_submitted, is_correct

G. SECURITY FEATURES
--------------------
- bcrypt password hashing
- JWT authentication
- Rate limiting (5 attempts/15 min)
- Account lockout (5 failed attempts)
- Input validation
- CSRF protection

H. TROUBLESHOOTING
------------------
Common Issues:
1. Database not created - Run app.py first
2. API keys missing - Check .env file
3. Port 5000 in use - Change port in app.py
4. Module not found - pip install -r requirements.txt

I. DEPLOYMENT NOTES
-------------------
Development: python app.py
Production: Use gunicorn or similar WSGI server
Database: SQLite (dev) / PostgreSQL (prod)
Static files: Serve via nginx in production

J. LEGAL DISCLAIMER
-------------------
This platform is for educational purposes only.
Practice ethical hacking responsibly.
Only test on systems you own or have permission to test.
Do not use techniques learned here for malicious purposes.